# {{PROJECT_NAME}}

Generated by `strictpy new` for Python 3.14.6.

## Set up

```bash
uv sync --locked
```

## Validate

```bash
uv run basedpyright --level warning --warnings .
uv run python -m unittest discover -s tests -v
strictpy check .
```

## Property tests

`tests/test_properties.py` contains a bounded starter invariant using exact-pinned Hypothesis. Replace it with observable domain properties rather than duplicating the implementation.

Hypothesis minimizes failures. Add important minimized failures as explicit `@example` cases if they must run on every machine; `.hypothesis/examples` may also be committed while the exact Hypothesis version remains pinned.

Run the program with:

```bash
uv run python -m {{MODULE_NAME}}.main
```
